/** Bases personalizadas usam os mesmos ícones Lucide que o espaço de trabalho. */
export {
  DEFAULT_WORKSPACE_ICON as DEFAULT_CUSTOM_DATABASE_ICON,
  normalizeWorkspaceIconKey as normalizeDatabaseIconKey,
  resolveWorkspaceIcon as resolveCustomDatabaseIcon,
  WORKSPACE_ICON_PRESETS as DATABASE_ICON_PRESETS,
} from "@/modules/workspace/workspaceIcons";
